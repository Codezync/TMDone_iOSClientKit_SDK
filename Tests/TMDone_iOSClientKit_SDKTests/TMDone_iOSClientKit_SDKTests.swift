import XCTest
@testable import TMDone_iOSClientKit_SDK

final class TMDone_iOSClientKit_SDKTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TMDone_iOSClientKit_SDK().text, "Hello, World!")
    }
}
